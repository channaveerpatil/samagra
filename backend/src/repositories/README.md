# Repositories

This directory will hold data-access repositories once a database is introduced.

Intentionally empty in this phase — no database has been added yet (see `docs/BACKEND_REQUIREMENTS.md`).
